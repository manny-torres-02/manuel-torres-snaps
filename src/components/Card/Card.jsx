const Card = ({ id, photoDescription, index }) => {
  return (
    <>
      <div>
        <h1>{id}</h1>
        <p>{photoDescription}</p>
        test
      </div>
    </>
  );
};

export default Card;
